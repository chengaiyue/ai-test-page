{
  "$schema": "https://json.schemastore.org/tsconfig",
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    // 组件库：由 Rollup 负责 JS 打包，TS 仅产出 .d.ts 声明文件
    "noEmit": false,
    "declaration": true,
    "emitDeclarationOnly": true
  }
}
